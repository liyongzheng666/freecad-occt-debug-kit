{
  "_note": "合成 S3 fixture case（A7 WP3）：直接跑 near-tangent fixture 的 ssi_probe，验证 investigate() 的 fixture 路径与 _ssi_verdict fired 分支在 eval 路径被覆盖。无真实 fillet 几何失败现场——诚实标注为 fixture/合成。真实 S3 接触退化现场（StartSol 过 + PerformSurf 内接触曲线退化 + 面具名）两轮 7 族未获（见 WP2），留作后续长杆。",
  "case_id": "s3-fixture",
  "input": {
    "builder": "s3-fixture",
    "note": "合成 fixture case：无真实几何——ssi_fixture 字段触发 investigate() 走 fixture 路径"
  },
  "agent_run": {
    "case": "s3-fixture",
    "radius": 1.0,
    "ssi_fixture": "near-tangent",
    "_note": "ssi_fixture 触发 investigate() 直接跑 ssi_probe(fixture='near-tangent')，跳过 reproduce/radius_probe；near-tangent fixture = 圆柱侧面 × 紧贴平面（x=10.1>r=10）：夹角≈0° + section_edges=0 → s3_signature=true → fired。"
  },
  "ground_truth": {
    "true_chain": ["S3"],
    "entities": [],
    "entities_human": "合成 fixture case：无真实 fillet 失败现场，实体维 N/A（fixture 路径跳过 LLDB capture）。",
    "expected_evidence": "ssi_probe(fixture='near-tangent')：近切夹角<5° + section_edges=0 → s3_signature=true → S3 fired",
    "aligned_fix": "heal 近切支撑面使接触曲线非退化（增大二面角），或降半径至 blend 面能与两支撑面正常接触",
    "failure_class": "algorithmic_overflow",
    "_failure_class_note": "fixture 代表 blend 面接触退化型 S3（PerformSurf 内求交失败），与 box-r5 的 StripeEdgeInter 2D pcurve overlap 同属 algorithmic_overflow 大类。",
    "mechanism_truth": {
      "signature": "s3_degenerate_contact",
      "observable": { "n_section_edges": 0, "s3_signature": true },
      "basis": "合成 fixture（诚实标注非真实失败现场）：ssi_probe 近切<5° + section_edges=0 → S3 接触退化。P1b 机制真分（此 case 的机制真值来自 fixture 定义，非 truth-run 抓取）。",
      "stage_reached": "S3"
    }
  }
}
